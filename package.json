{
    "scripts": {
        "docker-start": "cls && docker-compose up -d --build",
        "docker-stop": "cls && docker-compose down",
        "docker-terminal": "cls && docker exec -it frankenphp bash",
        "phpcs": "cls && docker exec -it frankenphp bash -c 'cd public && vendor/bin/phpcs --standard=PSR2 ./classes/* ./index.php'"
    }
}